# Copy to .env.local (for `vercel dev`) or set these as real env vars in your
# Vercel project settings. See README → "Spotify Now Playing" for how to get
# each value. Never commit the real file — .env* is gitignored.